@using WebApplication3.Models
@model List<User>
<h1>Blog</h1>

<table class="table table-striped table-dark">
	<thead>
		<tr>
			<th scope="col">ad</th>
			<th scope="col">SoyAd</th>
			<th scope="col">Gmail</th>
			<th scope="col">Shifre</th>
		</tr>
	</thead>
	<tbody>
		 @foreach (User user in Model)
            {
                <tr>
                    <td>@user.Name</td>
                    <td>@user.Surname</td>
                    <td>@user.Email</td>
                    <td>@user.Password</td> 
                </tr>
            }
	</tbody>

</table>